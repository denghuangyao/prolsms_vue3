{
  "name": "dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm vite --mode development",
    "build": "pnpm vite build --mode production",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "@dhy/plugins": "workspace:*"
  }
}
