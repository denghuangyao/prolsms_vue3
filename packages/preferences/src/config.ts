import type { Preferences } from '@dhy/types';

const defaultPreferences: Preferences = {
  app: {
    defaultHomePath: '/workbenches',
  },
  tabbar: {
    maxCount: 10,
  },
};
export { defaultPreferences };
